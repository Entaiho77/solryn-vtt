// Public API of @solryn/systems: the system registry (lookup/list) over the bundled game systems.
// Individual system data files (dnd5e/*, solryn/*) remain importable via @solryn/systems/<path>.
export * from './registry';
